DATABASE_URL=postgresql://user:pass@localhost:5432/metabuild
# Optional when using Accelerate/Data Proxy
# PRISMA_ACCELERATE_URL=https://accelerate.example.com/...
API_KEY=dev-api-key
ALLOWED_ORIGIN=http://localhost:5173
RATE_LIMIT_PER_MIN=120
AI_EMBEDDINGS_URL=http://localhost:8000
AI_EMBEDDINGS_TIMEOUT_MS=8000

# SMTP Email Configuration (optional - for sending quotes/invoices via email)
# Leave blank to disable email functionality (falls back to mailto: links)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM=TicTacStick <noreply@yourdomain.com>
